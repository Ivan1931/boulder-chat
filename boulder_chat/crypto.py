from .server import a_method

def the_truth():
    return a_method()

# Method to encrypt a message given the user's private key
def encrypt(msg, pri_key):

# Method to decrypt a message given the sender's public key
def decrypt(msg, pub_key):
